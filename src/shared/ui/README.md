# Shared UI

Canonical shared UI entrypoints for Phase 2 migration.

- Use `@/shared/ui` for new imports.
- Canonical shared UI primitives — import `@/shared/ui/*` (legacy root `components/ui` removed).
- Target state (Phase 2.2+): move implementations into `src/shared/ui/*`.
