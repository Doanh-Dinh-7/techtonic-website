/** @deprecated Import from `@/3d` — transitional re-export bridge (Phase 3.1). */
export {
  CanvasShell,
  WebGLFallback,
  CameraRig,
  ParticleField,
  CatMascot,
  FloatingLogo,
  BackgroundScene,
  HeroScene,
  HeroSceneLazy,
  BackgroundSceneLazy,
} from "@/3d";
