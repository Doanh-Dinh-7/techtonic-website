# Shared Types

Application-wide contracts centralized for Phase 2+.

- `common.ts`: baseline generic types (`Nullable`, `Optional`, `BaseEntity`).
- `ui.ts`: shared UI-centric contracts (e.g. `ThemeMode`, `ToastVariant`).
