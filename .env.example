# Tuỳ chọn: URL form đăng ký ngoài (Google Form, v.v.). Nếu không set, nút "Tham gia" dẫn tới /recruitment.
NEXT_PUBLIC_REGISTER_URL=
NEXT_PUBLIC_FORM_ID=
NEXT_PUBLIC_GG_FORM=

# Org GitHub chung của CLB (hiển thị trang Dự án).
NEXT_PUBLIC_GITHUB_ORG=https://github.com/techtonicclub

# --- Chatbot TechTonic (server-only) ---
# Gemini API key (không public).
GEMINI_API_KEY=

# Supabase project URL (không public).
SUPABASE_URL=

# Supabase service role key (không public; dùng trong API routes).
SUPABASE_SERVICE_ROLE_KEY=

# (Tuỳ chọn) Embedding model cho Gemini. Mặc định: gemini-embedding-001 và gemini-2.5-flash-lite
GEMINI_EMBED_MODEL=gemini-embedding-001
GEMINI_CHAT_MODEL=gemini-2.5-flash-lite

# Bật/tắt form đăng ký tuyển thành viên. true = hiển thị form, false = ẩn form
NEXT_PUBLIC_IS_HIRING=true

# Deadline tuyển thành viên (ISO 8601).
# OPEN: thời điểm bắt đầu nhận đơn | CLOSE: thời điểm đóng đơn
NEXT_PUBLIC_RECRUITMENT_OPEN=2026-09-14T00:00:00+07:00
NEXT_PUBLIC_RECRUITMENT_CLOSE=2026-09-28T00:00:00+07:00

# Chế độ giao diện: light | dark | both
# - light / dark: khóa theme, ẩn nút toggle
# - both: cho phép người dùng chuyển tự do
NEXT_PUBLIC_THEME_MODE=both
